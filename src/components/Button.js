import React from 'react';

const Button = () => {

    return (
        <>
            <button>
                This is still a button
            </button>
        </>
    )
}

export default Button;