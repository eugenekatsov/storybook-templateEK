import React from 'react';

const Button = () => {

    return (
        <>
            <button>
                This is a now larger button
            </button>
        </>
    )
}

export default Button;